{
    "title": "Event",
    "description": "The protocol defining what an Event payload looks like.\n\nAll events subclass EventCls.",
    "type": "object",
    "properties": {
        "event_name": {
            "title": "Event Name",
            "type": "string"
        }
    },
    "required": [
        "event_name"
    ]
}