{
    "title": "Command",
    "description": "The protocol defining what a Command payload looks like.\n\nAll commands subclass CommandCls.",
    "type": "object",
    "properties": {
        "command_name": {
            "title": "Command Name",
            "type": "string"
        }
    },
    "required": [
        "command_name"
    ]
}